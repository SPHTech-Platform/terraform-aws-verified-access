formatter: "markdown table"

#recursive:
#  enabled: true
#  path: modules

output:
  file: "README.md"
  mode: replace
  template: |
    # Terraform AWS Verified Access

    {{ .Content }}

settings:
  lockfile: false
